import createTheme from "@mui/material/styles/createTheme";

const theme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#0066cc",
    },
    secondary: {
      main: "#ffcc00",
    },
  },
});

export default theme;
